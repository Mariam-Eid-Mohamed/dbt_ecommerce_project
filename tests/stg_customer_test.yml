version: 2

models:
  - name: stg_customers
    description: "Staging table for customers"
    columns:
      - name: email
        description: "Customer email address"
        tests:
          - not_null
          - unique